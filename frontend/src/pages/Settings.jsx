function SettingsPage() {
    return (
        <section className="card">
            <h2>Settings</h2>
            <p className="muted-text">Notification and personalization settings are planned for next iteration.</p>
        </section>
    );
}

export default SettingsPage;
